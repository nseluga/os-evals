You are in a git repository. Do these steps in order, using tools:

1. Create a file named `smoke.txt` containing exactly the single line: SMOKE_OK
2. Read `smoke.txt` back to confirm its contents.
3. Create a new git branch named `smoke-branch` and commit `smoke.txt` on it.

Then reply with a one-line confirmation. Keep it minimal — this is a plumbing smoke test.
