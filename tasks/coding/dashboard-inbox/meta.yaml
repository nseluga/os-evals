category: coding
contaminated: false
group: clean
source: project-dashboard PLAN.md Item 3.2 (built via /dev-team-auto session 51c6abdf, 2026-07-08)
description: |
  Real task: implement Item 3.2 "Quick-capture inbox UI" of the project-dashboard —
  a spec Nate wrote in PLAN.md and shipped via dev-team-auto (commit c8dee19). This is
  the gold-standard check type: the repo's OWN test suite is the pass/fail gate.

  Contaminated: false. group: clean.
  Rationale: the check is a behavioral test (tests/inbox-ui.test.ts) that imports the
  real route handlers and asserts POST-persists / DELETE-removes / empty-state / renders.
  It rewards a CORRECT implementation, not knowledge of ~/os. The PLAN references
  ~/portfolio/STANDARDS.md for *style*, but the test checks *behavior*, not style, so
  passing does not depend on having those docs. (Flagged in the audit as a mild
  knowledge-adjacency to watch, not a contamination.)

  Frozen workspace: project-dashboard @ 5a4c800 (parent of the feature commit c8dee19).
  At this base, src/lib/manual.ts and src/pages/api/inbox.ts already exist; the model
  builds only the UI (src/components/Inbox.astro + wiring into src/pages/index.astro).

  Check type: run the vendored tests/inbox-ui.test.ts under vitest against WORKSPACE_DIR.
  Requires: (a) the workspace-restore contract extension (WORKSPACE_DIR), and
  (b) node_modules provisioned in the restored workspace (npm ci). Heavier than a
  text/filesystem check.

  STATUS: NEEDS-VALIDATION — I have not yet confirmed the vendored test passes on the
  real solution commit from the frozen base (to prove it is a fair, achievable gate).
  Do not trust as a scoring gate until that dry-run is done.
