This workspace has a static `index.html` for a small dashboard header. It has several
accessibility problems. Fix the accessibility of `index.html` in place while preserving
the visible content and the actions available. Specifically make sure:

- Every image has meaningful alternative text.
- Every control has an accessible name (an icon-only button needs an accessible label;
  a text field needs an associated label).
- Anything the user can click to perform an action is a real, keyboard-operable control
  (not a bare non-interactive element with a click handler).

Edit `index.html` only. Keep the same actions and text the page already offers.
