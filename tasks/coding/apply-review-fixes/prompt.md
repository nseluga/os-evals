This workspace has `handler.py` and a code review in `REVIEW.md`. Apply **all** the
fixes called for in `REVIEW.md` to `handler.py`, preserving the module's behavior and
public function signatures. Edit `handler.py` in place. Do not delete the functions or
change what they are meant to do — just fix the issues the review raises.
